# standalone egui fractal-clock

Takes the fractal clock example from the [egui repository](https://github.com/emilk/egui/blob/0.33.0/crates/egui_demo_app/src/apps/fractal_clock.rs) and makes it standalone. 
It can be run with `cargo run --release` in this directory.